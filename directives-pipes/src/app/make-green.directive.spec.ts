import { MakeGreenDirective } from './make-green.directive';

describe('MakeGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeGreenDirective();
    expect(directive).toBeTruthy();
  });
});
